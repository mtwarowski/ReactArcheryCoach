const initialState = {
    daily: {date: "", scores: [], practices: []},
}

const intensities = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default intensities;